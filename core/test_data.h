#include <QString>

static const QString EXAMPLE_CONTEXT_TEXT = QString(R"(
Here's the text from the images, combined, along with the problem and a Python solution:\n\n**Text from the Images:**\n\n*   **41. First Missing Positive**\n*   Hard\n*   Topics\n*   Companies\n*   Hint\n*   Given an unsorted integer array `nums`, Return the smallest positive integer that is not present in `nums`.\n*   You must implement an algorithm that runs in *O(n)* time and uses *O(1)* auxiliary space.\n\n*   **Example 1:**\n\n    `Input: nums = [1,2,0]`\n    `Output: 3`\n    `Explanation: The numbers in the range [1,2] are all in the array.`\n\n*   **Example 2:**\n\n    `Input: nums = [3,4,-1,1]`\n    `Output: 2`\n    `Explanation: 1 is in the array but 2 is missing.`\n\n*   **Example 3:**\n\n    `Input: nums = [7,8,9,11,12]`\n    `Output: 1`\n    `Explanation: The smallest positive integer 1 is missing.`\n\n*   **Constraints:**\n    *   `1 <= nums.length <= 10^5`\n    *   `-2^31 <= nums[i] <= 2^31 - 1`\n\n**Problem:**\n\nFind the smallest missing positive integer in an unsorted array of integers, with the constraints that the algorithm must run in *O(n)* time and use *O(1)* auxiliary space.\n\n**Python Solution:**\n\n```python\ndef firstMissingPositive(nums):\n    \"\"\"\n    Finds the first missing positive integer in an unsorted array.\n\n    Args:\n        nums: A list of integers.\n\n    Returns:\n        The smallest missing positive integer.\n    \"\"\"\n    n = len(nums)\n\n    # 1. Check if 1 is present in the array. If not, you're done and 1 is the answer.\n    if 1 not in nums:\n        return 1\n\n    # 2. Replace negative numbers, zeros,\n    # and numbers larger than n by 1s.\n    # After this conversion, `nums` will contain\n    # only positive numbers.\n    for i in range(n):\n        if nums[i] <= 0 or nums[i] > n:\n            nums[i] = 1\n\n    # 3. Use the index as a hash key and number sign as a presence detector.\n    # For example, if nums[1] is negative, that means that the number `1`\n    # is present in the array.\n    # If nums[2] is positive, the number 2 is missing.\n    for i in range(n):\n        a = abs(nums[i])\n        # If you meet number a in the array, change the sign of the a-th element.\n        # Be careful with duplicates: do it only once.\n        if a == n:\n            nums[0] = - abs(nums[0])\n        else:\n            nums[a] = - abs(nums[a])\n\n    # 4. Now the index of the first positive number\n    # is equal to the first missing positive.\n    for i in range(1, n):\n        if nums[i] > 0:\n            return i\n\n    if nums[0] > 0:\n        return n\n\n    return n + 1\n```\n\n**Explanation of the Python Solution:**\n\n1.  **Base Case:**  Check if 1 exists in the array.  If not, 1 is the answer.\n\n2.  **Cleaning the Array:** Replace all negative numbers, zeros, and numbers greater than `n` (the length of the array) with 1.  This ensures we're only dealing with positive numbers that could potentially be the missing positive integer.\n\n3.  **Using Index as a Hash Key:**  This is the clever part.  We iterate through the array. For each number `a`, we change the sign of the element at index `a`.  This marks the presence of `a` in the array.  If `a` is equal to `n`, we change the sign of `nums[0]` instead because index `n` doesn't exist. We use `abs()` to avoid changing the sign twice for the same number.\n\n4.  **Finding the Missing Positive:**  Iterate through the array again, starting from index 1.  The first index `i` where `nums[i]` is positive indicates that the number `i` is missing.  If all `nums[1]` to `nums[n-1]` are negative, check if `nums[0]` is positive.  If so, `n` is missing. If not, `n+1` is missing.\n\n**Time and Space Complexity:**\n\n*   **Time Complexity:** O(n) -  We iterate through the array a constant number of times.\n*   **Space Complexity:** O(1) - We modify the array in place and use only a few constant extra variables.\n
)").replace("\\n", "\n");

static const QString EXAMPLE_CPP_TEXT = QString(R"(
"Okay, let's tackle a classic \"average\" LeetCode problem. We'll pick a problem that requires a common technique like Dynamic Programming (DP) or a clever two-pointer approach.\n\nLet's go with a variation of a classic DP problem often called \"House Robber\".\n\n---\n\n## LeLoot Adventure\n\n### Problem Description\n\nYou are on an adventure to collect LeetCoins from a series of special \"Loot Boxes\" arranged in a line. Each loot box `i` contains `loot[i]` LeetCoins. You have a peculiar rule: you cannot collect from two adjacent loot boxes. If you collect from box `i`, you *cannot* collect from box `i-1` or `i+1`. Your goal is to maximize the total LeetCoins you can collect.\n\n**Example 1:**\n```\nInput: loot = [2, 7, 9, 3, 1]\nOutput: 12\nExplanation: Collect loot from box 1 (7), then box 3 (3), then box 5 (1). Total = 7 + 3 + 1 = 11.\nAlternatively, collect loot from box 0 (2), then box 2 (9), then box 4 (1). Total = 2 + 9 + 1 = 12.\nThe maximum is 12.\n```\n\n**Example 2:**\n```\nInput: loot = [1, 2, 3, 1]\nOutput: 4\nExplanation: Collect loot from box 1 (1) and then box 3 (3). Total = 1 + 3 = 4.\n```\n\n**Constraints:**\n*   `1 <= loot.length <= 100`\n*   `0 <= loot[i] <= 400`\n\n---\n\n### Approach: Dynamic Programming\n\nThis problem exhibits optimal substructure and overlapping subproblems, making it a perfect candidate for Dynamic Programming.\n\nLet `dp[i]` represent the maximum LeetCoins you can collect considering loot boxes from index `0` up to index `i`.\n\nWhen we are at loot box `i`, we have two choices:\n\n1.  **Collect from loot box `i`:** If we choose to collect `loot[i]`, we *cannot* collect from `loot[i-1]`. Therefore, the maximum loot we could have collected before `loot[i]` would be `dp[i-2]`. So, the total in this case is `loot[i] + dp[i-2]`.\n2.  **Do not collect from loot box `i`:** If we choose not to collect from `loot[i]`, then the maximum loot we can collect up to `i` is simply the maximum loot collected up to `i-1`, which is `dp[i-1]`.\n\nCombining these, the recurrence relation is:\n`dp[i] = max(dp[i-1], loot[i] + dp[i-2])`\n\n#### Base Cases:\n\n*   **`n = 0` (empty array):** Although constraints say `loot.length >= 1`, if it were allowed, the result would be 0.\n*   **`n = 1` (one loot box):** We can only collect from this one box. `dp[0] = loot[0]`.\n*   **`n = 2` (two loot boxes):** We can collect from either `loot[0]` or `loot[1]`, but not both. So, `dp[1] = max(loot[0], loot[1])`.\n\n#### Detailed Steps for DP:\n\n1.  Handle edge cases for `n=0` and `n=1`.\n2.  Initialize a `dp` array of size `n`.\n3.  Set `dp[0] = loot[0]`.\n4.  Set `dp[1] = max(loot[0], loot[1])`.\n5.  Iterate from `i = 2` to `n-1`:\n    `dp[i] = max(dp[i-1], loot[i] + dp[i-2])`\n6.  The final result will be `dp[n-1]`.\n\n### Space Optimization\n\nNotice that `dp[i]` only depends on `dp[i-1]` and `dp[i-2]`. This means we don't need to store the entire `dp` array. We can reduce the space complexity from `O(N)` to `O(1)` by only keeping track of the previous two maximum values.\n\nLet's use:\n*   `prev2` to store `dp[i-2]`\n*   `prev1` to store `dp[i-1]`\n*   `current_max` to calculate `dp[i]`\n\n**Optimized Steps:**\n\n1.  Handle edge cases for `n=0` and `n=1`.\n2.  Initialize `prev2 = loot[0]`.\n3.  Initialize `prev1 = max(loot[0], loot[1])`.\n4.  Iterate from `i = 2` to `n-1`:\n    a.  `current_max = max(prev1, loot[i] + prev2)`\n    b.  Update `prev2 = prev1` (for the next iteration, `prev1` becomes `prev2`)\n    c.  Update `prev1 = current_max` (for the next iteration, `current_max` becomes `prev1`)\n5.  The final result will be `prev1`.\n\n### C++ Solution\n\n```cpp\n#include <vector>\n#include <algorithm> // Required for std::max\n\nclass Solution {\npublic:\n    int maxLeetCoins(std::vector<int>& loot) {\n        int n = loot.size();\n\n        // Edge case: No loot boxes\n        if (n == 0) {\n            return 0;\n        }\n\n        // Edge case: Only one loot box\n        if (n == 1) {\n            return loot[0];\n        }\n\n        // --- Dynamic Programming with Space Optimization ---\n\n        // prev2 represents dp[i-2]\n        // For i = 0, this is loot[0]\n        int prev2 = loot[0];\n\n        // prev1 represents dp[i-1]\n        // For i = 1, this is max(loot[0], loot[1])\n        int prev1 = std::max(loot[0], loot[1]);\n\n        // Iterate from the third loot box (index 2)\n        for (int i = 2; i < n; ++i) {\n            // current_max represents dp[i]\n            // It's the maximum of:\n            // 1. Not collecting current loot[i] (which means the max is prev1)\n            // 2. Collecting current loot[i] (which means loot[i] + max from two steps back, prev2)\n            int current_max = std::max(prev1, loot[i] + prev2);\n\n            // Update prev2 and prev1 for the next iteration\n            prev2 = prev1;         // The old prev1 becomes the new prev2\n            prev1 = current_max;   // The current max becomes the new prev1\n        }\n\n        // After the loop, prev1 holds the maximum loot collected up to the last box (dp[n-1])\n        return prev1;\n    }\n};\n\n```\n\n### Complexity Analysis\n\n*   **Time Complexity:** O(N)\n    *   We iterate through the `loot` array once from start to end. Each step involves constant time operations (comparisons, additions, assignments). `N` is the number of loot boxes.\n*   **Space Complexity:** O(1)\n    *   We only use a few extra variables (`n`, `prev2`, `prev1`, `current_max`) regardless of the input array size. This is constant space. If we used the full `dp` array, it would be O(N).\n\n---\n\nThis problem is a great example of how Dynamic Programming can be applied to optimization problems, and how space optimization techniques can significantly improve its memory footprint. It's a very common pattern found in \"average\" difficulty LeetCode problems."
)").replace("\\n", "\n");

static const QString EXAMPLE_MULTIPLECODE_TEXT = QString(R"(
"Отличный запрос! Создадим пример конфигурации для гипотетического приложения, демонстрирующий различные типы данных (строки, числа, булевы значения, списки, вложенные объекты) во всех трёх форматах.\n\nСценарий: Настройки веб-приложения, включая параметры сервера, базы данных и функции.\n\n---\n\n### 1. Файл `config.yaml` (YAML)\n\n**Что такое YAML?**\nYAML (YAML Ain't Markup Language) — это человекочитаемый стандарт сериализации данных, часто используемый для файлов конфигурации и обмена данными между языками. Он использует отступы для обозначения структуры и является супермножеством JSON.\n\n**`config.yaml`:**\n```yaml\n# config.yaml\n\n# Общие настройки приложения\napplication_name: MyAwesomeApp\nversion: 1.0.5\ndebug_mode: true\nlog_level: INFO\n\n# Настройки сервера\nserver:\n  host: 127.0.0.1\n  port: 8080\n  timeout_seconds: 30\n  enable_https: false\n\n# Настройки базы данных\ndatabase:\n  type: PostgreSQL\n  host: db.example.com\n  port: 5432\n  username: app_user\n  password: \"secure_password_123\" # Строки с особыми символами лучше брать в кавычки\n  max_connections: 50\n  tables:\n    - users\n    - products\n    - orders\n\n# Настройки функций\nfeatures:\n  analytics_enabled: true\n  notification_channels:\n    - email\n    - sms\n    - push\n  rate_limit:\n    enabled: true\n    requests_per_minute: 100\n  supported_languages: [en, ru, es, fr] # Также можно указывать списки в одну строку\n\n# Список администраторов (пример простого списка строк)\nadministrators:\n  - alice\n  - bob\n  - charlie\n```\n\n---\n\n### 2. Файл `config.toml` (TOML)\n\n**Что такое TOML?**\nTOML (Tom's Obvious, Minimal Language) — это формат файла конфигурации, который призван быть легко читаемым благодаря семантике, которая очевидна и прямолинейна. Он отображается в хеш-таблицу (словарь в Python).\n\n**`config.toml`:**\n```toml\n# config.toml\n\n# Общие настройки приложения\napplication_name = \"MyAwesomeApp\"\nversion = 1.0.5\ndebug_mode = true\nlog_level = \"INFO\"\n\n# Настройки сервера (секция обозначается квадратными скобками)\n[server]\nhost = \"127.0.0.1\"\nport = 8080\ntimeout_seconds = 30\nenable_https = false\n\n# Настройки базы данных (вложенные секции обозначаются точками)\n[database]\ntype = \"PostgreSQL\"\nhost = \"db.example.com\"\nport = 5432\nusername = \"app_user\"\npassword = \"secure_password_123\"\nmax_connections = 50\ntables = [\"users\", \"products\", \"orders\"] # Списки (массивы)\n\n# Настройки функций\n[features]\nanalytics_enabled = true\nnotification_channels = [\"email\", \"sms\", \"push\"]\nsupported_languages = [\"en\", \"ru\", \"es\", \"fr\"]\n\n# Вложенная секция для rate_limit\n[features.rate_limit]\nenabled = true\nrequests_per_minute = 100\n\n# Список администраторов (простой массив)\nadministrators = [\"alice\", \"bob\", \"charlie\"]\n```\n\n---\n\n### 3. Python код (`read_config.py`)\n\nЭтот код покажет, как прочитать и обработать файлы `config.yaml` и `config.toml` с помощью соответствующих библиотек.\n\n**Предварительные требования:**\nДля запуска этого кода вам понадобятся следующие библиотеки Python:\n```bash\npip install pyyaml toml\n```\n(Для Python 3.11+ `toml` можно заменить на встроенный `tomllib` для чтения, но `toml` все равно хорош для записи и более старых версий Python).\n\n**`read_config.py`:**\n```python\n# read_config.py\nimport yaml\nimport toml\nimport os\n\n# --- 1. Создание файлов конфигурации для демонстрации ---\n\ndef create_dummy_config_files():\n    \"\"\"Создает config.yaml и config.toml для демонстрации, если их нет.\"\"\"\n\n    yaml_content = \"\"\"\n# config.yaml\n\n# Общие настройки приложения\napplication_name: MyAwesomeApp\nversion: 1.0.5\ndebug_mode: true\nlog_level: INFO\n\n# Настройки сервера\nserver:\n  host: 127.0.0.1\n  port: 8080\n  timeout_seconds: 30\n  enable_https: false\n\n# Настройки базы данных\ndatabase:\n  type: PostgreSQL\n  host: db.example.com\n  port: 5432\n  username: app_user\n  password: \"secure_password_123\"\n  max_connections: 50\n  tables:\n    - users\n    - products\n    - orders\n\n# Настройки функций\nfeatures:\n  analytics_enabled: true\n  notification_channels:\n    - email\n    - sms\n    - push\n  rate_limit:\n    enabled: true\n    requests_per_minute: 100\n  supported_languages: [en, ru, es, fr]\n\n# Список администраторов (пример простого списка строк)\nadministrators:\n  - alice\n  - bob\n  - charlie\n    \"\"\"\n\n    toml_content = \"\"\"\n# config.toml\n\n# Общие настройки приложения\napplication_name = \"MyAwesomeApp\"\nversion = 1.0.5\ndebug_mode = true\nlog_level = \"INFO\"\n\n# Настройки сервера\n[server]\nhost = \"127.0.0.1\"\nport = 8080\ntimeout_seconds = 30\nenable_https = false\n\n# Настройки базы данных\n[database]\ntype = \"PostgreSQL\"\nhost = \"db.example.com\"\nport = 5432\nusername = \"app_user\"\npassword = \"secure_password_123\"\nmax_connections = 50\ntables = [\"users\", \"products\", \"orders\"]\n\n# Настройки функций\n[features]\nanalytics_enabled = true\nnotification_channels = [\"email\", \"sms\", \"push\"]\nsupported_languages = [\"en\", \"ru\", \"es\", \"fr\"]\n\n# Вложенная секция для rate_limit\n[features.rate_limit]\nenabled = true\nrequests_per_minute = 100\n\n# Список администраторов\nadministrators = [\"alice\", \"bob\", \"charlie\"]\n    \"\"\"\n\n    # Записываем YAML файл\n    yaml_filepath = 'config.yaml'\n    if not os.path.exists(yaml_filepath):\n        with open(yaml_filepath, 'w', encoding='utf-8') as f:\n            f.write(yaml_content.strip())\n        print(f\"Создан файл: {yaml_filepath}\")\n    else:\n        print(f\"Файл {yaml_filepath} уже существует.\")\n\n    # Записываем TOML файл\n    toml_filepath = 'config.toml'\n    if not os.path.exists(toml_filepath):\n        with open(toml_filepath, 'w', encoding='utf-8') as f:\n            f.write(toml_content.strip())\n        print(f\"Создан файл: {toml_filepath}\")\n    else:\n        print(f\"Файл {toml_filepath} уже существует.\")\n\n# --- 2. Функции для чтения конфигураций ---\n\ndef load_yaml_config(filepath):\n    \"\"\"\n    Загружает конфигурацию из YAML файла.\n    Возвращает словарь Python.\n    \"\"\"\n    try:\n        with open(filepath, 'r', encoding='utf-8') as file:\n            config = yaml.safe_load(file)\n        print(f\"\\n--- Конфигурация YAML успешно загружена из {filepath} ---\")\n        return config\n    except FileNotFoundError:\n        print(f\"Ошибка: YAML файл не найден по пути: {filepath}\")\n        return None\n    except yaml.YAMLError as e:\n        print(f\"Ошибка при парсинге YAML файла {filepath}: {e}\")\n        return None\n\ndef load_toml_config(filepath):\n    \"\"\"\n    Загружает конфигурацию из TOML файла.\n    Возвращает словарь Python.\n    \"\"\"\n    try:\n        with open(filepath, 'r', encoding='utf-8') as file:\n            config = toml.load(file)\n        print(f\"\\n--- Конфигурация TOML успешно загружена из {filepath} ---\")\n        return config\n    except FileNotFoundError:\n        print(f\"Ошибка: TOML файл не найден по пути: {filepath}\")\n        return None\n    except toml.TomlDecodeError as e:\n        print(f\"Ошибка при парсинге TOML файла {filepath}: {e}\")\n        return None\n\n# --- 3. Основная логика выполнения ---\n\nif __name__ == \"__main__\":\n    create_dummy_config_files()\n\n    yaml_filepath = 'config.yaml'\n    toml_filepath = 'config.toml'\n\n    print(\"\\n\" + \"=\"*50)\n    print(\"ЧТЕНИЕ И ОБРАБОТКА YAML КОНФИГУРАЦИИ\")\n    print(\"=\"*50)\n\n    yaml_config = load_yaml_config(yaml_filepath)\n\n    if yaml_config:\n        print(\"\\nПримеры доступа к данным из YAML:\")\n        print(f\"  Название приложения: {yaml_config.get('application_name')}\")\n        print(f\"  Режим отладки: {yaml_config.get('debug_mode')}\")\n        \n        # Доступ к вложенным данным\n        server_settings = yaml_config.get('server', {})\n        print(f\"  Хост сервера: {server_settings.get('host')}\")\n        print(f\"  Порт сервера: {server_settings.get('port')}\")\n\n        db_settings = yaml_config.get('database', {})\n        print(f\"  Тип базы данных: {db_settings.get('type')}\")\n        print(f\"  Максимальное количество соединений: {db_settings.get('max_connections')}\")\n        print(f\"  Таблицы БД: {', '.join(db_settings.get('tables', []))}\")\n\n        features = yaml_config.get('features', {})\n        print(f\"  Аналитика включена: {features.get('analytics_enabled')}\")\n        print(f\"  Каналы уведомлений: {', '.join(features.get('notification_channels', []))}\")\n        \n        rate_limit_settings = features.get('rate_limit', {})\n        print(f\"  Лимит запросов в минуту: {rate_limit_settings.get('requests_per_minute')}\")\n\n        print(f\"  Администраторы: {', '.join(yaml_config.get('administrators', []))}\")\n\n        print(\"\\nПолный объект конфигурации YAML (как словарь Python):\")\n        import json\n        print(json.dumps(yaml_config, indent=2, ensure_ascii=False))\n\n\n    print(\"\\n\" + \"=\"*50)\n    print(\"ЧТЕНИЕ И ОБРАБОТКА TOML КОНФИГУРАЦИИ\")\n    print(\"=\"*50)\n\n    toml_config = load_toml_config(toml_filepath)\n\n    if toml_config:\n        print(\"\\nПримеры доступа к данным из TOML:\")\n        print(f\"  Название приложения: {toml_config.get('application_name')}\")\n        print(f\"  Версия приложения: {toml_config.get('version')}\")\n        \n        # Доступ к вложенным данным\n        server_settings = toml_config.get('server', {})\n        print(f\"  Хост сервера: {server_settings.get('host')}\")\n        print(f\"  Включен HTTPS: {server_settings.get('enable_https')}\")\n\n        db_settings = toml_config.get('database', {})\n        print(f\"  Имя пользователя БД: {db_settings.get('username')}\")\n        print(f\"  Пароль БД: {db_settings.get('password')}\") # В реальном приложении пароли лучше не выводить!\n\n        features = toml_config.get('features', {})\n        print(f\"  Поддерживаемые языки: {', '.join(features.get('supported_languages', []))}\")\n        \n        rate_limit_settings = features.get('rate_limit', {}) # Доступ к вложенной секции\n        print(f\"  Лимит скорости включен: {rate_limit_settings.get('enabled')}\")\n\n        print(f\"  Администраторы: {', '.join(toml_config.get('administrators', []))}\")\n\n        print(\"\\nПолный объект конфигурации TOML (как словарь Python):\")\n        import json\n        print(json.dumps(toml_config, indent=2, ensure_ascii=False))\n\n    # Опционально: очистка созданных файлов после демонстрации\n    # if os.path.exists(yaml_filepath):\n    #     os.remove(yaml_filepath)\n    #     print(f\"\\nУдален файл: {yaml_filepath}\")\n    # if os.path.exists(toml_filepath):\n    #     os.remove(toml_filepath)\n    #     print(f\"Удален файл: {toml_filepath}\")\n```\n\n---\n\n### Как запустить Python код:\n\n1.  **Сохраните файлы:**\n    *   Создайте файл `config.yaml` и скопируйте в него содержимое YAML.\n    *   Создайте файл `config.toml` и скопируйте в него содержимое TOML.\n    *   Создайте файл `read_config.py` и скопируйте в него код Python.\n    *   Убедитесь, что все три файла находятся в одной директории.\n    (Или просто запустите `read_config.py`, он сам создаст dummy-файлы).\n\n2.  **Установите зависимости:**\n    Откройте терминал или командную строку в той же директории и выполните:\n    ```bash\n    pip install pyyaml toml\n    ```\n\n3.  **Запустите Python скрипт:**\n    ```bash\n    python read_config.py\n    ```\n\nВы увидите вывод, показывающий, как Python загружает и интерпретирует данные из YAML и TOML файлов в стандартные словари и списки Python, а также примеры доступа к этим данным."
)").replace("\\n", "\n").replace("\\\"", "\"");

static const QString EXAMPLE_MULTIPLECODE_TEXT2 = QString::fromLatin1(R"(
"Let's solve the problem of determining if a given positive integer is a **prime number**.\n\n---\n\n### Common Description of the Solution\n\n**Problem:** Determine if a given positive integer `n` is a prime number.\nA prime number is a natural number greater than 1 that has no positive divisors other than 1 and itself.\n\n**Algorithm (Common Logic):**\n\n1.  **Handle Edge Cases:**\n    *   If `n` is less than or equal to 1, it is not prime. Return `false`.\n    *   If `n` is exactly 2, it is prime. Return `true`.\n    *   If `n` is greater than 2 and is an even number, it is not prime (since it would be divisible by 2). Return `false`.\n\n2.  **Iterative Check (for odd numbers greater than 2):**\n    *   Since we've handled 2 and all other even numbers, we only need to check for odd divisors starting from 3.\n    *   We don't need to check all numbers up to `n-1`. A key optimization is that if a number `n` has a divisor `d`, then `n/d` is also a divisor. If `d > sqrt(n)`, then `n/d` must be ` < sqrt(n)`. This means we only need to check for divisors up to the square root of `n`.\n    *   Start a loop from `i = 3` and continue as long as `i * i <= n`.\n    *   In each step of the loop, increment `i` by 2 (to check only odd numbers).\n    *   Inside the loop, if `n` is perfectly divisible by `i` (i.e., `n % i == 0`), then `n` has a divisor other than 1 and itself, so it is not prime. Return `false`.\n\n3.  **Conclusion:**\n    *   If the loop completes without finding any divisors, it means `n` has no divisors other than 1 and itself (within the checked range), so it is prime. Return `true`.\n\n---\n\n### C++ Code\n\n```cpp\n#include <iostream> // For input/output operations (cin, cout)\n#include <cmath>    // For the sqrt function (though optimized here with i*i <= n)\n\n// Function to check if a number is prime\nbool isPrime(int n) {\n    // 1. Handle edge cases\n    if (n <= 1) {\n        return false; // Numbers less than or equal to 1 are not prime\n    }\n    if (n == 2) {\n        return true;  // 2 is the only even prime number\n    }\n    if (n % 2 == 0) {\n        return false; // Other even numbers are not prime\n    }\n\n    // 2. Iterative check for odd divisors from 3 up to sqrt(n)\n    // We use i * i <= n instead of i <= sqrt(n) to avoid floating-point math\n    // and potential precision issues, and it's often slightly faster.\n    for (int i = 3; i * i <= n; i += 2) {\n        if (n % i == 0) {\n            return false; // Found a divisor, so it's not prime\n        }\n    }\n\n    // 3. If no divisors found, it's prime\n    return true;\n}\n\nint main() {\n    int num;\n    std::cout << \"Enter a positive integer: \";\n    std::cin >> num;\n\n    if (isPrime(num)) {\n        std::cout << num << \" is a prime number.\" << std::endl;\n    } else {\n        std::cout << num << \" is not a prime number.\" << std::endl;\n    }\n\n    return 0;\n}\n```\n\n**How to Compile and Run (C++):**\n1.  Save the code as `prime_checker.cpp`.\n2.  Open a terminal or command prompt.\n3.  Compile using a C++ compiler (like g++):\n    `g++ prime_checker.cpp -o prime_checker`\n4.  Run the executable:\n    `./prime_checker` (on Linux/macOS) or `prime_checker.exe` (on Windows)\n\n---\n\n### Python Code\n\n```python\nimport math # For math.isqrt (integer square root) or math.sqrt\n\n# Function to check if a number is prime\ndef is_prime(n):\n    # 1. Handle edge cases\n    if n <= 1:\n        return False # Numbers less than or equal to 1 are not prime\n    if n == 2:\n        return True  # 2 is the only even prime number\n    if n % 2 == 0:\n        return False # Other even numbers are not prime\n\n    # 2. Iterative check for odd divisors from 3 up to sqrt(n)\n    # math.isqrt(n) provides the integer square root, which is perfect for this loop limit.\n    # The range function's end is exclusive, so we add 1 to include the square root if it's an integer.\n    for i in range(3, math.isqrt(n) + 1, 2):\n        if n % i == 0:\n            return False # Found a divisor, so it's not prime\n\n    # 3. If no divisors found, it's prime\n    return True\n\n# Main part to get input and display result\nif __name__ == \"__main__\":\n    try:\n        num = int(input(\"Enter a positive integer: \"))\n        if is_prime(num):\n            print(f\"{num} is a prime number.\")\n        else:\n            print(f\"{num} is not a prime number.\")\n    except ValueError:\n        print(\"Invalid input. Please enter an integer.\")\n\n```\n\n**How to Run (Python):**\n1.  Save the code as `prime_checker.py`.\n2.  Open a terminal or command prompt.\n3.  Run the script using the Python interpreter:\n    `python prime_checker.py`\n\n---\n\n### C# Code\n\n```csharp\nusing System; // For Console for input/output, and Math for sqrt\n\npublic class PrimeChecker\n{\n    // Function to check if a number is prime\n    public static bool IsPrime(int n)\n    {\n        // 1. Handle edge cases\n        if (n <= 1)\n        {\n            return false; // Numbers less than or equal to 1 are not prime\n        }\n        if (n == 2)\n        {\n            return true;  // 2 is the only even prime number\n        }\n        if (n % 2 == 0)\n        {\n            return false; // Other even numbers are not prime\n        }\n\n        // 2. Iterative check for odd divisors from 3 up to sqrt(n)\n        // We use i * i <= n instead of i <= Math.Sqrt(n) to avoid floating-point math\n        // and potential precision issues, and it's often slightly faster.\n        for (int i = 3; i * i <= n; i += 2)\n        {\n            if (n % i == 0)\n            {\n                return false; // Found a divisor, so it's not prime\n            }\n        }\n\n        // 3. If no divisors found, it's prime\n        return true;\n    }\n\n    // Main method where the program execution begins\n    public static void Main(string[] args)\n    {\n        Console.Write(\"Enter a positive integer: \");\n        string input = Console.ReadLine();\n        int num;\n\n        // Try to parse the input string to an integer\n        if (int.TryParse(input, out num))\n        {\n            if (IsPrime(num))\n            {\n                Console.WriteLine($\"{num} is a prime number.\");\n            }\n            else\n            {\n                Console.WriteLine($\"{num} is not a prime number.\");\n            }\n        }\n        else\n        {\n            Console.WriteLine(\"Invalid input. Please enter an integer.\");\n        }\n    }\n}\n```\n\n**How to Compile and Run (C#):**\n1.  Save the code as `PrimeChecker.cs`.\n2.  Open a terminal or command prompt.\n3.  Compile using the .NET SDK (assuming it's installed):\n    `dotnet build` (if you create a new console project with `dotnet new console` first and then replace the `Program.cs` content, or copy this code into an existing one)\n    Or, for a single file:\n    `dotnet run PrimeChecker.cs` (This command directly compiles and runs C# source files with .NET 6+)\n    Alternatively, for older .NET versions or more traditional compilation:\n    `csc PrimeChecker.cs`\n    `PrimeChecker.exe`\n\n---"
)").replace("\\n", "\n").replace("\\\"", "\"");
